// src/game/engine/playerAction.engine.ts
import { PlayerAction, PlayerActionResult } from "../models/playerAction";
import { GameState } from "../models/state";


export function processPlayerAction(state: GameState, action: PlayerAction): PlayerActionResult {
return 
}