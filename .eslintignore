# Monorepo build artifacts and generated code
**/node_modules/**
**/dist/**
**/build/**
**/db-entities-dist/**

# Non-source packages not intended for linting from workspace root
common/db-entities/**

# Exclude package metadata from eslint validation to avoid config lookup failures
*.json
