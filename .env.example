# Environment (determines nginx config selection and backend behavior)
NODE_ENV=development # or production

# Database password (used in docker-compose.prod.yml)
DB_PASSWORD=transcendence # CHANGE THIS IN PRODUCTION

# Backend services
AUTH_SERVICE_PORT=3001

# Frontend build-time variables (currently unused - frontend uses relative paths)
# VITE_API_URL=http://localhost/api # dev
# VITE_API_URL=https://valinor.ink/api # prod
# VITE_WS_URL=ws://localhost/ws # dev
# VITE_WS_URL=wss://valinor.ink/ws # prod
TEST=1
