include:
  - docker-compose.yml

services:
  # === Core Application ===

  frontend:
    build: ./frontend
    container_name: frontend-dev
    volumes:
      - ./frontend/package.json:/app/package.json:ro
      - ./frontend/tsconfig.json:/app/tsconfig.json:ro
      - ./frontend/vite.config.js:/app/vite.config.js:ro
      - ./frontend/public:/app/public:rw
      - ./frontend/src:/app/src:rw
    ports:
      - "5173:5173"
    networks:
      - transcendence_net
    command: npm run dev

  core:
    build:
      context: .
      dockerfile: backend/core/Dockerfile
    container_name: core-dev
    volumes:
      - ./database:/database:ro
      - ./backend/core/src:/app/backend/core/src:ro
      - ./backend/core/package.json:/app/backend/core/package.json:ro
      - ./backend/core/tsconfig.json:/app/backend/core/tsconfig.json:ro
      - ./backend/core/nest-cli.json:/app/backend/core/nest-cli.json:ro
    ports:
      - "3000:3000"
    networks:
      - transcendence_net
    command: npm run start:dev

  # === Microservices ===

  auth-service:
    build:
      context: .
      dockerfile: ./backend/auth-service/Dockerfile
      target: development
    container_name: auth-service-dev
    ports:
      - "3001:3001"
    volumes:
      - ./backend/auth-service/src:/app/./backend/auth-service/src:ro
      - ./backend/auth-service/package.json:/app/./backend/auth-service/package.json:ro
      - ./backend/auth-service/tsconfig.json:/app/./backend/auth-service/tsconfig.json:ro
      - ./backend/auth-service/nest-cli.json:/app/./backend/auth-service/nest-cli.json:ro
    networks:
      - transcendence_net
    command: npm run start:dev

  api-gateway:
    build:
      context: ./backend/api-gateway
      dockerfile: Dockerfile
      target: development
    container_name: api-gateway-dev
    ports:
      - "3002:3002"
    volumes:
      - ./backend/api-gateway/src:/app/src:ro
      - ./backend/api-gateway/package.json:/app/package.json:ro
      - ./backend/api-gateway/tsconfig.json:/app/tsconfig.json:ro
      - ./backend/api-gateway/nest-cli.json:/app/nest-cli.json:ro
    networks:
      - transcendence_net
    command: npm run start:dev

  game-service:
    build:
      context: ./backend/game-service
      dockerfile: Dockerfile
      target: development
    container_name: game-service-dev
    ports:
      - "3003:3003"
    volumes:
      - ./backend/game-service/src:/app/src:ro
      - ./backend/game-service/package.json:/app/package.json:ro
      - ./backend/game-service/tsconfig.json:/app/tsconfig.json:ro
      - ./backend/game-service/nest-cli.json:/app/nest-cli.json:ro
    networks:
      - transcendence_net
    command: npm run start:dev

  # === Infrastructure ===

  db:
    container_name: postgres-dev
    ports:
      - "5432:5432"
    volumes:
      - db_data_dev:/var/lib/postgresql/data
      - ./database/init/schema/00-schema.sql:/docker-entrypoint-initdb.d/00-schema.sql:ro
    networks:
      - transcendence_net

  nginx:
    build: ./nginx
    container_name: nginx-dev
    ports:
      # - "80:80" // comment out port for compatability with hive's computers
      - "8080:8080"
    networks:
      - transcendence_net
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://127.0.0.1:8080/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  db_data_dev:
